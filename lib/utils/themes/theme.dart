import 'package:flutter/material.dart';

applicationTheme(){
  return ThemeData(
    primaryColor: Colors.orange,
    primaryColorDark: Color(0xfffda37e),
    secondaryHeaderColor: Colors.white,
  );
}